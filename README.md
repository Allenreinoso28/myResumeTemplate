# myResumeTemplate
My Resume Template for 2025/25 Applicaiton Season


✅ Do this inside your project folder:
python3 -m venv .venv


This creates a private Python environment called .venv.

✅ Activate it:
source .venv/bin/activate


Your terminal prompt will now look like:

(.venv) allenreinoso@Allens-Air %

✅ Now install PyPDF2 normally:
pip install PyPDF2

✅ Run your script:
python combine.py